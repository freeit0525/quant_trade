# 量化交易系统功能说明

> 更新日期：2026-08-12（买卖点预测：命中口径统一为后5日收益率 + 随机入场基线，信号阈值调优 35/-35，因子降维 13→12 停用基本面）
> 本文档梳理系统整体功能与页面职责；买卖点预测模型的最新迭代状态另见 `predict-model-summary.md`。

---

## 1. 系统概览

- 单文件 Python 后端 `server.py`（`http.server`，默认端口 8000）+ 多个静态 HTML 页面
- 浏览器访问入口：`http://127.0.0.1:8000/`
- 数据存远程 PostgreSQL（库名 `quant_trading`），所有页面均从库读数据，拉取时自动增量入库
- 技术指标（MA/MACD/RSI/KDJ/乖离率/量比等）在入库时统一计算，前端直接读取

## 2. 运行方式

```bash
.venv\Scripts\python.exe server.py --port 8000
```

数据库配置通过项目根目录 `.env` 提供（DB_HOST/DB_PORT/DB_NAME/DB_USER/DB_PASSWORD），代码无硬编码。

## 3. 页面功能一览

| 页面 | 入口 | 功能职责 |
|---|---|---|
| index.html | `/` | 入口导航页，列出全部功能卡片 |
| kline.html | K线展示 | 查看K线、MA均线、成交量副图，支持时间范围/缩放 |
| fetch.html | 数据拉取 | 指定数据源（东财/腾讯等）拉取K线与资金流，接口可用性探测、字段缺失检测 |
| backtest.html | 策略回测 | 运行 MACD/均线等传统策略回测，输出买卖点、波段收益、净值曲线，结果可保存 |
| strategy.html | 策略管理 | 管理已保存回测策略：新建/编辑/删除/一键加载 |
| macd.html | MACD计算 | 计算 MACD/DIF/DEA，金叉死叉信号图表 |
| predict.html | 买卖点预测 | **核心功能**：预测下一个交易日的操作建议（加仓/持有/观望/减仓/卖出），见第 4 节 |
| predict-backtest.html | 买卖点回测 | 全量历史数据批量回测 12 因子预测模型：信号命中率、N日收益、逐因子有效性分析 |
| predict-strategy.html | 买卖点策略回测 | 把买卖点信号做成完整策略：次日开盘入场、T+1、止损止盈、手续费，输出资金曲线与盈亏比 |

## 4. 买卖点预测功能（predict.html）

### 4.1 功能定位

**预测下一个交易日该如何操作**：通过过去的交易日数据（12 大因子，基本面因子已停用），输出单一操作建议，并给出对应的买卖时机、次日走势形态与分时预案。结论可落库，次日自动复盘统计命中率。

### 4.2 操作建议体系（2026-08-11 改版）

预测结论统一为 5 类操作指令：

| 操作 | code | 含义 | 复盘命中口径 |
|---|---|---|---|
| 加仓 | add | 偏多信号强，可分批买入 | 次日上涨 = 命中 |
| 持有 | hold | 趋势偏多，持股待涨 | 只记录涨跌，不计命中 |
| 观望 | watch | 方向不明，等待确认 | 只记录涨跌，不计命中 |
| 减仓 | trim | 偏空，分批减仓 | 次日下跌 = 命中 |
| 卖出 | sell | 偏空信号强，清仓离场 | 次日下跌 = 命中 |

判定依据（`predict-engine.js` 的 `opVerdict(Sbuy, Ssell)`，Sbuy=买入向评分、Ssell=卖出向评分，区间 ±100）：

1. 加仓（强）：`Sbuy ≥ 25 且 Ssell ≥ -10`
2. 加仓（中）：`Sbuy ≥ 12 且 Ssell ≥ -15`
3. 持有：`Sbuy ≥ 5 且 Ssell > -12`
4. 卖出：`Sbuy ≤ -25 或 Ssell ≤ -25`
5. 减仓：`Sbuy ≤ -12 或 Ssell ≤ -10`
6. 其余 → 观望

### 4.3 评分模型

- **12 大因子**（各 ±100 分，正分利多、负分利空；2026-08-12 因子降维，基本面因子已停用）：主力成交、成交量、换手率、量比、RSI、KDJ、乖离率、MACD、K线形状、均线、OBV、短期涨跌幅(mom5)
- **买入/卖出两套独立权重**（可在页面滑块调整，默认权重基于全库回测调优）
- 综合评分 `S = Σ(因子分 × 权重) / Σ权重`，Sbuy/Ssell 各算一次
- 次日上涨概率 `P_up = clamp(50 + Sbuy/2, 5, 95)`

### 4.4 页面输出内容

1. **一句话结论摘要**（💡）：按操作建议给出通俗解读，含超跌反弹/超涨回落提示
2. **操作建议卡片**：主操作 + 买入向/卖出向评分 + 次日涨跌概率
3. **买卖时机**：回踩支撑位/买入区间/突破追高 + 上方压力/卖出区间/止损位
4. **因子明细**：12 因子实时评分条（基本面已停用，权重0不计入总分），点击可查看打分依据
5. **明日走势形态预测**：9 种形态（放量大涨→放量大跌）概率分布
6. **明日操作策略**：量能/MACD 场景建议 + 分时预案（高开/平开/低开）+ 关键价位预案
7. **K线与买卖信号图**：历史买入/卖出信号标记 + 预测标记
8. **历史信号明细**：最近 12 条信号及 5 日表现
9. **预测落库与复盘**：保存结论 → 次日自动复盘（命中/走势验证）→ 统计命中率与因子有效性 → 迭代建议

### 4.5 信号判定与回测口径

- 历史信号：`computeSignalsDual` 按 Sbuy 跃升上穿 `BUY_TH=35` 记买入信号、Ssell 跃升下穿 `SELL_TH=-35` 记卖出信号（2026-08-12 由 25/-25 调优，阈值可自定义传入）
- 命中口径（`signalStats`，predict.html 展示；2026-08-12 统一为主口径 + 辅助）：
  - **主口径（权威，后5日收益率）**：买入信号后 5 日收盘上涨 = 命中；卖出信号后 5 日收盘下跌 = 命中；同时输出买卖信号后 5 日均收益，并与**随机入场基线**（`baselineStats`：该股全历史任意一天后 5 日上涨占比/均收益）对比超额
  - **辅助口径（盘中触及）**：买入信号后 5 日盘中最高触及 +2% = 命中；卖出信号后 5 日盘中最低触及 -2% = 命中（高波动股天然偏高，仅参考）
- 全库回测（50 只全池，后5日收益率口径）：阈值 35/-35 后买入命中率 60.4%（随机基线 49.1%），均收益 +1.79%；卖出信号后 5 日均收益 -0.68%（基线 +0.55%，负收益=卖出后下跌、信号有效）；信号数较原阈值减半、策略级总收益 +170%→+340%

### 4.5.1 大盘波动率过滤（2026-08-12 新增，替换无效的超跌过滤）

- 指数数据：`scripts/fetch_index.py`（baostock 拉沪深300 sh.000300 全历史入库，2005 至今约 5186 根）；`/api/index_kline` 返回指数 K 线，库内尾部缺口自动增量拉取
- 过滤逻辑（`predict-engine.js`）：`computeMarketState` 按指数 K 线算每日状态（近5日动量 / 站上MA20 / 近20日年化波动率）；`marketFilterAllow` 默认要求**年化波动率适中 15%~45%**；`computeSignalsDual` 第 7 参 opts 传入 marketStates 后仅过滤买入信号（卖出不过滤，弱势/过热市卖出信号更需保留；指数缺失日视为通过）
- 回测依据（50 只全池+沪深300）：过滤后策略 Calmar 0.28→0.47、回撤 28.4%→20.7%、年化 7.98%→9.72%；样本外(2023后) Calmar 0.14→0.19 仍提升
- 页面接入：predict-backtest.html / predict-strategy.html 提供"大盘波动率过滤"开关（默认开）并在状态栏显示剔除信号数；predict.html 展示"大盘状态"卡（动量/波动率/趋势/买入放行）

### 4.6 落库与复盘闭环

- `POST /api/predict/save`：同一股票同一依据日重复保存覆盖
- `GET /api/predictions`：预测记录（自动触发复盘未复盘记录）
- `GET /api/predictions/stats`：汇总统计（累计预测/已复盘/总体命中率/买入卖出命中率/均收益/因子有效性/迭代建议）
- 复盘命中判定（`database/db.py`）：加仓(add)次日上涨=命中；减仓/卖出(trim/sell)次日下跌=命中；持有/观望(hold/watch)只记录涨跌不计命中；走势命中=次日实际形态与预测形态一致

## 5. 数据源与回退链

`data/fetcher.py` 默认 source=auto（akshare）回退链：**akshare → 东财直连 → baostock → 腾讯**。

| 数据源 | 换手率 | 资金流 | 备注 |
|---|---|---|---|
| akshare（东财） | ✅ | ✅ | 走东财接口 |
| 东财直连 | ✅ | ✅ | 易被风控，已加 Chrome 指纹/多主机轮询/重试退避 |
| baostock | ✅ | ❌ | 免费稳定独立源，部分历史日期 turn 为空 |
| 腾讯 | ❌ | ❌ | 备用源，无换手率（增量写入已做 NULL 保护） |

资金流回退链：**东财直连 → akshare → 新浪**（新浪口径差异大，仅作兜底）。

## 6. 数据流

```
浏览器页面 ──(/api/kline)──▶ server.py ──▶ DataFetcher.fetch_stock
                                              ├─ 查库最大/最小日期，定位缺口
                                              ├─ 只拉缺口（尾部+头部），end 钳制到今天
                                              ├─ 增量重算指标 + 合并资金流
                                              └─ upsert 入库
浏览器 ◀── JSON(含全量K线+指标) ── server.py ◀── database/db.py（get_kline）
```

## 7. 常见 API

- `/api/kline`：拉取（增量）+ 入库 + 返回
- `/api/kline_db`：只读库
- `/api/index_kline`：大盘指数日K线（默认沪深300 sh.000300，缺口自动增量拉取，供市场过滤）
- `/api/stock_catalog`：stock_info 模糊搜索（`?keyword=`）
- `/api/stock_list` / `/api/search` / `/api/stock_info` / `/api/sources` / `/api/fetch`
- `/api/predict/save`、`/api/predictions`、`/api/predictions/stats`
- `/api/backtest/save`、`/api/strategies`
